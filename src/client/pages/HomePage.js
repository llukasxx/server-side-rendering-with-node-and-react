import React from "react";

const HomePage = () => {
  return (
    <div>
      <div>I am the home dsa</div>
      <button onClick={() => console.log("click me")}>Click me</button>
    </div>
  );
};

export default {
  component: HomePage
};
